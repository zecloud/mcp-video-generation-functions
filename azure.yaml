# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mcp-video-generation-functions
metadata:
  template: functions-quickstart-python-azd@1.0.1
services:
  api:
    project: ./src/
    language: python
    host: function
